
<!-- include header -->
    <%- include('include/_header') %>
<!-- /include header -->
    <!-- Main Site -->
    <main id="site-main">
        <div class="container">
            <div>
                <div class="wrap">
            <div class="member-header">
                MEMBERS
            </div> 
         <div>
            <form method="POST" action="/search-user" class="search" id="search">
                <input  type="search" placeholder="Search" name="lastname" aria-label="Search"  class="searchTerm">
                <button ype="submit" class="searchButton"><i class="fa fa-search"></i></button>
            </form>
         </div>
        </div>
            <div class="box-nav d-flex justify-between" id="add-member">
                <a href="/add-user" class="border-shadow">
                    <span class="text-gradient">Add New Member  <i class="fa fa-user fa_custom"></i></span>
                </a>
            </div>

            <!-- form handling -->
            <% if(users.length > 0){%>
            <form action="/" method="POST">
                <table class="table">
                    <thead class="thead-dark">
                        <tr>
                            <th>ID</th>
                            <th>First Name</th>
                            <th>Last Name</th>
                            <th>Email Id</th>
                            <th>Gender</th>
                            <th>Status</th>
                            <th>Action</th>
                        </tr>
                    </thead>
                    <tbody>
                        <%- include('include/_show') %>
                    </tbody>
                </table>
            </form>
            <% } else { %> 
                <h2 style="text-align: center; margin-top: 2em;" class="text-gradient"> Members Data not found! </h2>
                <% } %> 
        </div>
    </main>
<!-- /Main Site -->


<!-- include footer -->
<%- include('include/_footer') %>
<!-- /include footer -->